import React from 'react';
import styles from './header.module.scss';

const Header = () => (
  <div className={styles.container}>
    <div className={styles.header}>Craig Gordon</div>
  </div>
);

export default Header;